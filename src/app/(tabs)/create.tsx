import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const CreateScreen = () => {
  return (
    <SafeAreaView>
      <Text className="text-red-500 text-center">Create Screen</Text>
    </SafeAreaView>
  );
};

export default CreateScreen;
